apiVersion: v1
kind: Service
metadata:
  name: {{ template "authorcoresearchapi.fullname" . }}
  labels:
    app: {{ template "authorcoresearchapi.name" . }}
    chart: {{ template "authorcoresearchapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "authorcoresearchapi.name" . }}
    release: {{ .Release.Name }}
